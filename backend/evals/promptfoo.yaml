providers:
  - id: http
    config:
      url: http://localhost:4000/api/completions
      method: POST
      headers:
        Content-Type: application/json
      body:
        prompt: "{{prompt}}"

prompts:
  - "{{prompt}}"

tests:
  - file://datasets/sample.json
